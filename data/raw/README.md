Place the raw AQI CSV files in this folder if you want to keep all training data inside the AirSense AI project tree.
The loader also searches the workspace root, so the current files still work without moving them immediately.
